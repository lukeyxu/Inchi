<%= form_for(@testitem) do |f| %>
  <% if @testitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testitem.errors.count, "error") %> prohibited this testitem from being saved:</h2>

      <ul>
      <% @testitem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
