<div class="section-nav">
  <h2>Inchi</h2>
  <nav class="lots_nav">
    <div class="mainnav">
      <a href="">Apartment Info</a>
      <a href="">Classfied</a>
    </div>
    <div class="loginnav">
<%if !user_signed_in?%>
      <a href="/login">Log in</a>
      <a href="/signup">Sign up</a>
<% end %>
<%if user_signed_in?%>
      <a href="">Welcome</a>
      <a href="/logout">Sign out</a>
<% end %>
    </div>
  </nav>
</div>
<p id="notice"><%= notice %></p>
<div class="container">
<h1>Item Listing</h1>
<table class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @testlots.each do |testlot| %>
      <tr>
        <td><%= testlot.name %></td>
        <td><%= testlot.description %></td>
        <td><%= testlot.status %></td>
        <td><%= link_to 'Show', testlot %></td>
        <td><%= link_to 'Edit', edit_testlot_path(testlot) %></td>
        <td><%= link_to 'Destroy', testlot, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Testlot', new_testlot_path %>

</div>